import React from 'react'
import { LoadingContainer, LoadingIndicator } from './Loading.styles'

export const Loading = () => {
  return (
    <LoadingContainer>
        <LoadingIndicator
            size={96}
         />
    </LoadingContainer>
  )
}
